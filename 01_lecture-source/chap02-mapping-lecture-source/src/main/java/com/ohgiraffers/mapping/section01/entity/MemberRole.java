package com.ohgiraffers.mapping.section01.entity;


public enum MemberRole {

    //enum : Enumerated type 의 줄임말이다.

    /* 필기. 전부 상수 이다 바뀔 수 없는 값
    *   Enum 타입을 사용하게 된다면 코드의 가독성 향상
    *   type safety (타입의 안정성) 을 보장할 수 있다.
    *   서로 연고나된 상수들의 집합 클래스이다. */
    ROLE_MEMBER,
    ROLE_ADMIN
}
